name: movie_db
description: To integrate all sorts of features provided by MovieDB website

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  # cached_network_image: 2.4.0
  cache_image: "^1.0.5"
  dartz: ^0.9.2
  dio: ^3.0.10
  equatable: ^1.2.6
  flutter:
    sdk: flutter
  flutter_bloc: ^6.1.2
  flutter_swiper: ^1.1.6
  get_it: ^5.0.6
  google_fonts: ^1.1.2
  transparent_image: ^1.0.0

  # window_size:
  #   git:
  #     url: git://github.com/google/flutter-desktop-embedding.git
  #     path: plugins/window_size

dev_dependencies:
  flutter_test:
    sdk: flutter
  import_sorter: ^4.4.1
  # integration_test:
  #   sdk: flutter
  lint: ^1.3.0
  mockito: ^4.1.4

flutter:
  uses-material-design: true
  assets:
    - assets/google_fonts/
    - assets/pngs/
